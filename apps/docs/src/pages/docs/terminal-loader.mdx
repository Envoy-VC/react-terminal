---
title: Terminal Loader
---

# Terminal Loader

The Loader component is used when a command is executing in a terminal.

If no children are passed, it defaults to a animation and text 'Executing Command...'

Example:

```tsx
<TerminalLoader>
  <p>Cusotm Loading...</p>
</TerminalLoader>
```
